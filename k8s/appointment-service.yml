apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointment-service
  namespace: rednorte
  labels:
    app: appointment-service
spec:
  replicas: 1
  revisionHistoryLimit: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: appointment-service
  template:
    metadata:
      labels:
        app: appointment-service
    spec:
      containers:
      - name: appointment-service
        image: gonzalo25u/rednorte-appointment-service:${IMAGE_TAG}
        ports:
        - containerPort: 8083
        envFrom:
        - configMapRef:
            name: rednorte-config
        - secretRef:
            name: rednorte-secrets
---
apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: rednorte
spec:
  selector:
    app: appointment-service
  ports:
  - port: 8083
    targetPort: 8083
  type: ClusterIP